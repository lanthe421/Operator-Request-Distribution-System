# ✅ СИСТЕМА ЗАПУЩЕНА И РАБОТАЕТ!

## 🚀 Текущий статус

**Сервер запущен:** http://localhost:8000

**Интерактивная документация:** http://localhost:8000/docs

## 📊 Текущее состояние системы

### Операторы (7 активных):
- **Operator_1**: 40% загрузки (4/10 обращений)
- **Иван Петров**: 10% загрузки (1/10 обращений)  
- **Мария Сидорова**: 0% загрузки (0/15 обращений)
- **Алексей Иванов**: 0% загрузки (0/8 обращений)
- И еще 3 оператора...

### Источники обращений (4):
- Test Source
- Telegram Bot
- Email Support
- Phone Support

### Обращения:
- Всего создано: 5+ обращений
- Все успешно распределены между операторами
- Система автоматически назначает операторов на основе весов

## 🎯 Как использовать

### 1. Откройте документацию
Перейдите в браузере: **http://localhost:8000/docs**

Там вы увидите все доступные API endpoints и сможете тестировать их прямо в браузере!

### 2. Создайте обращение

**Через Swagger UI (http://localhost:8000/docs):**
1. Найдите `POST /api/v1/requests`
2. Нажмите "Try it out"
3. Введите данные:
```json
{
  "user_identifier": "your_email@example.com",
  "source_id": 2,
  "message": "Ваше сообщение"
}
```
4. Нажмите "Execute"

**Через Python:**
```python
import requests

response = requests.post(
    "http://localhost:8000/api/v1/requests",
    json={
        "user_identifier": "user@example.com",
        "source_id": 2,
        "message": "Здравствуйте! Нужна помощь"
    }
)
print(response.json())
```

**Через curl (PowerShell):**
```powershell
$body = @{
    user_identifier = "user@example.com"
    source_id = 2
    message = "Здравствуйте! Нужна помощь"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8000/api/v1/requests" `
    -Method Post `
    -Body $body `
    -ContentType "application/json"
```

### 3. Посмотрите статистику

**Загрузка операторов:**
```
http://localhost:8000/api/v1/stats/operators-load
```

**Распределение обращений:**
```
http://localhost:8000/api/v1/stats/requests-distribution
```

### 4. Управляйте операторами

**Создать нового оператора:**
```json
POST /api/v1/operators
{
  "name": "Новый оператор",
  "max_load_limit": 20
}
```

**Деактивировать оператора:**
```
PUT /api/v1/operators/1/toggle-active
```

**Изменить лимит загрузки:**
```json
PUT /api/v1/operators/1
{
  "max_load_limit": 25
}
```

### 5. Настройте веса

Веса определяют вероятность назначения оператора (от 1 до 100):

```json
POST /api/v1/sources/2/operators
{
  "weights": [
    {"operator_id": 2, "weight": 50},
    {"operator_id": 3, "weight": 30},
    {"operator_id": 4, "weight": 20}
  ]
}
```

## 📚 Полная документация

Смотрите файл **USAGE_GUIDE_RU.md** для подробного руководства со всеми примерами!

## 🔧 Управление сервером

**Остановить сервер:**
- Нажмите `Ctrl+C` в терминале где запущен сервер

**Перезапустить сервер:**
```bash
python main.py
```

**Проверить работу:**
```bash
curl http://localhost:8000/health
```

## 💡 Примеры использования

### Сценарий 1: Массовое создание обращений
```python
import requests

for i in range(10):
    requests.post(
        "http://localhost:8000/api/v1/requests",
        json={
            "user_identifier": f"user{i}@example.com",
            "source_id": 2,
            "message": f"Обращение номер {i}"
        }
    )
```

### Сценарий 2: Мониторинг загрузки
```python
import requests

response = requests.get("http://localhost:8000/api/v1/stats/operators-load")
stats = response.json()

for op in stats:
    if op['load_percentage'] > 80:
        print(f"⚠️ Оператор {op['operator_name']} перегружен!")
```

### Сценарий 3: Балансировка нагрузки
```python
import requests

# Если оператор перегружен, увеличиваем его лимит
requests.put(
    "http://localhost:8000/api/v1/operators/1",
    json={"max_load_limit": 30}
)

# Или деактивируем на время
requests.put("http://localhost:8000/api/v1/operators/1/toggle-active")
```

## 🎉 Готово!

Система полностью настроена и готова к использованию!

**Что дальше:**
- Экспериментируйте с API через Swagger UI
- Создавайте обращения и смотрите как они распределяются
- Настраивайте веса для оптимального распределения
- Мониторьте загрузку операторов в реальном времени

**Нужна помощь?**
- Документация API: http://localhost:8000/docs
- Руководство: USAGE_GUIDE_RU.md
- Спецификация: .kiro/specs/operator-request-distribution/
